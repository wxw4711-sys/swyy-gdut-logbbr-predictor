# PythonAnywhere 免费版运行时依赖（预测接口所需，不含训练/gradio）
# 适配 Python 3.8：用 mordred==1.2.0（纯 Python，不依赖 numba，且不声明任何依赖，
# 因此 pip 不会自动拉第二份 rdkit 或 numba），只装一份 rdkit-pypi，可塞进 512MB 磁盘。
flask>=2.0
numpy>=1.21
pandas>=1.3
joblib>=1.1
lightgbm>=3.3
scikit-learn>=1.0
scipy>=1.7
rdkit-pypi==2021.9.1
mordred==1.2.0
# 重新训练模型时才需要下面这个包，预测接口不需要：
# optuna>=3.0
