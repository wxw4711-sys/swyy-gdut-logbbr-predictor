# PythonAnywhere 免费版运行时依赖（预测接口所需，不含训练/gradio）
# 注意：需在 Python 3.11 虚拟环境中安装，mordred-ojmb 要求 Python >= 3.11
flask>=2.0
numpy>=1.21
pandas>=1.3
joblib>=1.1
lightgbm>=3.3
scikit-learn>=1.0
scipy>=1.7
rdkit-pypi>=2022.03
mordred-ojmb>=1.3
# 重新训练模型时才需要下面这个包，预测接口不需要：
# optuna>=3.0
